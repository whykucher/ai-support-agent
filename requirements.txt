# Five packages, no vector DB, no ORM, no LLM SDKs.
# Ranges (not pins) so the project installs cleanly on Python 3.10 - 3.14.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0
pydantic>=2.6,<3.0
