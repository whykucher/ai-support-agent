# Normalise line endings: Windows checkouts get CRLF, the repository stores LF.
* text=auto eol=lf

# These must stay LF or they break inside Linux containers.
*.sh   text eol=lf
Dockerfile text eol=lf

# Keep CRLF for PowerShell so it opens cleanly in Notepad.
*.ps1  text eol=crlf
